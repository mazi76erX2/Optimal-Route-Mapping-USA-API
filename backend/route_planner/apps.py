from django.apps import AppConfig


class RoutePlannerConfig(AppConfig):
    """App configuration for route_planner"""

    default_auto_field = "django.db.models.BigAutoField"
    name = "route_planner"
